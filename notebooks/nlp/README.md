# راهنمای بخش پردازش زبان طبیعی

این پوشه مربوط به مسئله تحلیل احساس نظرات محصولات الکترونیکی آمازون است.

هدف این بخش، تحلیل داده‌های نظرات و طراحی مدلی برای پیش‌بینی امتیاز هر نظر از ۱ تا ۵ است.

کدهای این بخش در Google Colab اجرا می‌شوند.

متغیر هدف:

```text
overall
```

ورودی اصلی مدل:

```text
reviewText
```

در صورت ارائه دلیل مناسب، اطلاعات دیگری مانند `summary` نیز می‌توانند مورد استفاده قرار گیرند.

---

## محیط اجرا

Notebookهای این بخش در Google Colab اجرا می‌شوند.

Google Colab برای موارد زیر استفاده می‌شود:

- پاک‌سازی و تحلیل داده‌ها
- رسم نمودارها و Word Cloud
- آموزش مدل پایه
- استفاده از GPU برای آموزش مدل عمیق
- Fine-tune کردن مدل‌های پیش‌آموخته
- پیش‌بینی داده Test
- ساخت فایل Submission

داده‌ها، مدل‌ها و خروجی‌های حجیم در Google Drive ذخیره می‌شوند.

ساختار پیشنهادی Drive برای بخش NLP:

```text
AI_Project2/
├── data/
│   └── nlp/
├── models/
│   └── nlp/
└── outputs/
    └── nlp/
```

---

## اتصال Google Drive

در ابتدای Notebookهایی که به داده‌ها یا خروجی‌ها نیاز دارند، Drive متصل شود:

```python
from google.colab import drive

drive.mount("/content/drive")
```

مسیرهای پیشنهادی:

```python
PROJECT_ROOT = "/content/drive/MyDrive/AI_Project2"

NLP_DATA_DIR = f"{PROJECT_ROOT}/data/nlp"
NLP_MODEL_DIR = f"{PROJECT_ROOT}/models/nlp"
NLP_OUTPUT_DIR = f"{PROJECT_ROOT}/outputs/nlp"
```

در صورت متفاوت‌بودن محل پوشه مشترک، مسیرها باید اصلاح شوند.

---

## بخش‌های اصلی مسئله

- بررسی و پاک‌سازی داده‌ها
- بررسی توزیع امتیازها
- تحلیل متوازن یا نامتوازن بودن کلاس‌ها
- رسم Word Cloud
- تحلیل طول نظرات
- تحلیل کاربران دارای بیشترین رأی مفید
- تحلیل محصولات دارای بیشترین امتیاز ۵
- تحلیل برندهای دارای بیشترین تعداد نظر
- تحلیل نظرات مرتبط با گارانتی
- ساخت مدل پایه تحلیل احساس
- ساخت مدل عمیق تحلیل احساس
- تولید فایل Submission

---

## ترتیب Notebookها

| ترتیب | Notebook | مسئول | موضوع |
|---|---|---|---|
| 1 | `01_nlp_data_eda.ipynb` | فاطمه | آماده‌سازی داده و EDA |
| 2 | `02_nlp_product_analysis.ipynb` | بنیامین | تحلیل محصولات، برندها و کاربران |
| 3 | `03_nlp_baseline_sentiment.ipynb` | علی | مدل پایه تحلیل احساس |
| 4 | `04_nlp_deep_sentiment.ipynb` | عباس | مدل عمیق و Submission |
| 5 | `05_nlp_warranty_analysis.ipynb` | مهیا | تحلیل نظرات مرتبط با گارانتی |

---

## وابستگی مراحل

```text
داده پاک‌سازی‌شده و Split مشترک — فاطمه
                  │
     ┌────────────┼────────────┬────────────┐
     ↓            ↓            ↓            ↓
تحلیل محصولات   Baseline    تحلیل گارانتی   مدل عمیق
بنیامین          علی          مهیا           عباس
     │            │                           │
     └────────────┴──────────────┬────────────┘
                                 ↓
                    مقایسه و جمع‌بندی نهایی
```

نکات:

- تمام اعضا برای اجرای نهایی به داده پاک‌سازی‌شده فاطمه نیاز دارند.
- پس از آماده‌شدن داده، تسک‌های بنیامین، علی، مهیا و عباس می‌توانند موازی اجرا شوند.
- عباس برای شروع مدل عمیق لازم نیست منتظر تکمیل مدل پایه علی بماند.
- مدل پایه و مدل عمیق باید روی یک Split مشترک ارزیابی شوند.
- نتیجه مدل پایه برای مقایسه نهایی مورد استفاده قرار می‌گیرد.
- فایل‌های مشترک باید در Google Drive ذخیره شوند.

---

## خروجی هر مرحله

### مرحله اول: آماده‌سازی داده و EDA

مسئول: فاطمه

خروجی مورد انتظار:

```text
outputs/nlp/nlp_train_clean.csv
outputs/nlp/nlp_validation_clean.csv
outputs/nlp/nlp_test_clean.csv
```

وظایف اصلی:

- بارگذاری داده‌های Train و Test
- بارگذاری جدول محصولات
- بررسی ستون‌ها
- بررسی داده‌های گمشده
- پاک‌سازی داده‌ها
- بررسی توزیع `overall`
- ساخت دسته‌های مثبت، خنثی و منفی
- رسم Word Cloud
- بررسی طول `reviewText`
- ساخت Train/Validation Split مشترک

---

### مرحله دوم: تحلیل محصولات و برندها

مسئول: بنیامین

خروجی مورد انتظار:

- جدول ۱۰ کاربر دارای بیشترین مجموع `vote`
- جدول ۱۰ محصول دارای بیشترین تعداد امتیاز ۵
- جدول ۱۰ برند دارای بیشترین تعداد نظر
- میانگین امتیاز برندهای منتخب
- نمودارها و تفسیر نتایج

خروجی‌های قابل استفاده در Notebook نهایی در مسیر زیر ذخیره شوند:

```text
outputs/nlp/
```

---

### مرحله سوم: مدل پایه تحلیل احساس

مسئول: علی

مدل پایه پیشنهادی:

```text
TF-IDF + Logistic Regression
```

مدل کلاسیک دیگری نیز می‌تواند استفاده شود، به شرط آنکه دلیل انتخاب آن توضیح داده شود.

خروجی مورد انتظار:

```text
outputs/nlp/baseline_nlp_validation_predictions.csv
```

محتوای پیشنهادی:

```text
sample_id
true_label
predicted_label
```

---

### مرحله چهارم: مدل عمیق تحلیل احساس

مسئول: عباس

خروجی مورد انتظار:

```text
models/nlp/deep_sentiment_model/
outputs/nlp/deep_nlp_validation_predictions.csv
outputs/nlp/q2_submission.csv
```

وظایف اصلی:

- انتخاب مدل عمیق یا پیش‌آموخته
- Fine-tune کردن مدل روی داده پروژه
- ارزیابی روی Validation
- مقایسه با Baseline
- پیش‌بینی داده‌های Test
- ساخت Submission

---

### مرحله پنجم: تحلیل گارانتی

مسئول: مهیا

خروجی مورد انتظار:

- فهرست کلمات مرتبط با گارانتی
- نظرات فیلترشده
- جدول میانگین رضایت از گارانتی برای محصولات
- نام و برند محصولات
- توضیح محدودیت‌های روش

کلمات پایه:

```text
warranty
guarantee
```

کلمات مشابه، مترادف و غلط‌های املایی متداول نیز باید بررسی شوند.

---

## برنامه زمانی بخش NLP

### بعدازظهر ۹ مرداد

مسئول اصلی: فاطمه

- بارگذاری داده‌ها در Google Colab
- پاک‌سازی اولیه
- انجام EDA
- ساخت Train/Validation Split مشترک
- ذخیره داده آماده در Google Drive
- تحویل داده آماده به اعضای تیم

---

### ۱۰ مرداد

تسک‌های موازی:

- بنیامین: تحلیل محصولات، برندها و کاربران
- علی: مدل پایه تحلیل احساس
- مهیا: تحلیل نظرات گارانتی
- عباس: آماده‌سازی مدل عمیق

خروجی:

- تحلیل‌های اولیه
- Baseline اولیه
- نظرات اولیه مرتبط با گارانتی
- Pipeline اولیه مدل عمیق

---

### ۱۱ مرداد

مسئول اصلی مدل: عباس

- آموزش مدل عمیق در Google Colab
- ذخیره Checkpoint در Google Drive
- ارزیابی روی Validation
- مقایسه اولیه با Baseline
- تکمیل تحلیل‌های دیگر اعضا
- ذخیره Predictionها

---

### ۱۲ مرداد

مسئول: تمام اعضا

- انتخاب مدل نهایی
- پیش‌بینی داده‌های Test
- ساخت Submission
- اجرای کامل Notebookها در Google Colab
- تکمیل مستندات
- ساخت Notebook نهایی NLP

خروجی‌های نهایی:

```text
final/q2_amazon_reviews.ipynb
final/q2_submission.csv
```

---

## معیار ارزیابی

معیار اصلی ارزیابی مدل:

```text
Micro F1 Score
```

تمام مدل‌ها باید با استفاده از Validation Split مشترک مقایسه شوند.

---

## ساختار Submission

نام فایل:

```text
q2_submission.csv
```

فایل باید فقط شامل یک ستون باشد:

```text
predicted
```

نمونه:

```text
predicted
5
3
1
```

قوانین:

- ترتیب ردیف‌ها باید دقیقاً مطابق داده Test باشد.
- Index نباید ذخیره شود.
- مقادیر Prediction باید بین ۱ تا ۵ باشند.
- نام ستون باید دقیقاً `predicted` باشد.

---

## قوانین مشترک بخش NLP

- تمام مدل‌ها باید از Split مشترک استفاده کنند.
- Seed باید مشخص باشد.
- روش پاک‌سازی متن باید مستند شود.
- حذف یا نگهداری Stop Wordها باید دلیل داشته باشد.
- طول ورودی مدل باید بر اساس تحلیل داده انتخاب شود.
- روش مدیریت کلاس‌های نامتوازن باید توضیح داده شود.
- داده Test نباید در آموزش یا انتخاب مدل استفاده شود.
- معیار اصلی مقایسه Micro F1 است.
- Predictionهای Validation باید ذخیره شوند.
- مدل‌ها و داده‌های حجیم نباید در GitHub قرار بگیرند.
- فایل‌های حجیم باید در Google Drive ذخیره شوند.
- فایل Submission پیش از تحویل بررسی شود.
- Notebook باید پس از Restart Runtime قابل اجرا باشد.
- نصب کتابخانه‌های اضافی باید داخل Notebook نوشته شود.
- مسیرهای Drive باید در ابتدای Notebook قابل تنظیم باشند.
- Notebookها باید از ابتدا بدون خطا اجرا شوند.

---

## نکات مربوط به Google Colab

- فایل‌های ذخیره‌شده در `/content` موقتی هستند.
- مدل‌ها، داده‌های پردازش‌شده و Predictionها باید در Google Drive ذخیره شوند.
- Runtime ممکن است قطع شود؛ بنابراین Checkpointها باید در طول آموزش ذخیره شوند.
- برای مدل عمیق، GPU باید فعال باشد.
- پیش از اجرای کامل، Pipeline روی نمونه کوچکی از داده تست شود.
- Notebook نباید به مسیر شخصی و ثابت یک عضو وابسته باشد.

---

## گزارش فنی داخل Notebookها

هر Notebook باید شامل موارد زیر باشد:

- هدف
- ورودی و خروجی
- روش اجرا
- دلیل انتخاب روش‌ها
- پیش‌پردازش
- نمودارها و جدول‌ها
- نتایج
- تحلیل نتایج
- محدودیت‌ها
- جمع‌بندی

جزئیات فنی مدل‌ها و تحلیل‌ها باید داخل Notebook نوشته شوند، نه در README اصلی پروژه.

---

## وضعیت بخش NLP

- [ ] اتصال Google Colab به Drive
- [ ] بارگذاری داده‌ها
- [ ] پاک‌سازی داده‌ها
- [ ] انجام EDA
- [ ] ساخت Train/Validation Split
- [ ] تحلیل کاربران
- [ ] تحلیل محصولات
- [ ] تحلیل برندها
- [ ] ساخت مدل پایه
- [ ] ارزیابی Baseline
- [ ] تحلیل گارانتی
- [ ] ساخت مدل عمیق
- [ ] ارزیابی مدل عمیق
- [ ] مقایسه مدل‌ها
- [ ] پیش‌بینی داده Test
- [ ] ساخت Submission
- [ ] ساخت Notebook نهایی NLP
